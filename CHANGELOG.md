# Changelog

このプロジェクトの主な変更点を記録します。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョニングは [セマンティック バージョニング](https://semver.org/lang/ja/) を採用しています。

新しいバージョンを上に追記します。未リリースの変更は `[Unreleased]` にためていき、
リリース時にバージョン番号と日付へ繰り上げます。

## [Unreleased]

## [2.9.0] - 2026-09-22

### Added
- 左サイドバーの「登録チャンネル」で、ライブ配信中を示す赤いアイコンをクリックすると、チャンネルページを経由せずに配信中の動画へ直接移動できるようになりました（ポップアップの「ライブアイコンから配信へ直接移動」トグル、既定ON）。チャンネル名をクリックしたときは従来どおりチャンネルページへ移動します。⌘/Ctrl+クリックと中クリックでは新しいタブで開きます（通常のリンクと違い、開いたタブが前面になります）。

### Fixed
- `/@ハンドル名/live` や `/channel/<ID>/live` の形式のURLで「常に最新位置から再生」が動作していなかった不具合を修正。拡張機能がこれらのURLを対象にしていませんでした。

## [2.8.1] - 2026-09-22

### Fixed
- 「常に最新位置から再生」が実際には一度もシークしていなかった不具合を修正。シークに使っていたライブバッジは、ページを開いた直後は実際の再生位置に関わらず `disabled` になっており、`disabled` な要素へのクリックはイベントが発火しないため空振りしていました（Chrome Devで計測、読み込み後17秒間でクリックイベント0件）。保険として併記していたプレーヤーAPIの呼び出しも、実行世界（ISOLATED world）からはプレーヤーのメソッドが見えず、常に呼ばれていませんでした。

### Changed
- ライブヘッドへの自動シークを MAIN world へ移し、`content.js` を `live-bridge.js`（設定の受け渡し）と `live-inject.js`（シークの実行）に分割（画質機能と同じ構成）。ライブバッジのクリックはやめ、プレーヤーAPI（`seekToLiveHead()`）でシークし、`getProgressState().isAtLiveHead` で到達を確認するようにしました。
- 1回試して終わりではなく、ライブヘッドに到達するまで最大30秒リトライするようにしました。広告の再生中やプレーヤーの初期化中に開始しても取りこぼしません。

## [2.8.0] - 2026-09-22

### Added
- 左サイドバーの「登録チャンネル」で、ライブ配信中のチャンネルを一覧の先頭に並べ替える機能（ポップアップの「ライブ中のチャンネルを上に表示」トグル、既定ON）。「もっと見る」に隠れているチャンネルも対象で、配信中のものだけが上に引き上げられます。
- 左サイドバーの「登録チャンネル」を自動で展開したままにする機能（ポップアップの「登録チャンネルを常に展開」トグル、既定ON）。毎回「もっと見る」を押す必要がなくなります。

### Removed
- 設定トグルを切り替えたときのトースト通知（トグルの見た目で状態がわかるため不要）。

## [2.7.0] - 2026-08-29

### Added
- 動画の画質を自動設定する機能。別拡張だった yt-auto-quality-lite を統合したもので、ポップアップの「画質を自動設定」トグル（既定ON）・「常に最高画質を使う」・「デフォルト画質」（既定1080p）で切り替えられます。ライブ配信だけでなく通常動画・Shortsでも動作します。

### Changed
- ポップアップの設定を「ライブ配信」「画質」の2セクションに整理。
- 拡張機能の説明文を「YouTubeの視聴を快適にする便利機能を詰め合わせたChrome拡張機能」に変更（画質機能がライブ配信以外でも動作するため）。

## [2.6.2] - 2026-08-28

### Added
- ポップアップのヘッダー右端に現在のバージョンを表示。

## [2.6.1] - 2026-08-28

### Fixed
- 拡張機能を有効にしていると、通常の動画やショート動画（watchページで再生した場合）でシークバーが勝手に動画の最後まで進んで再生が終了してしまう不具合を修正。ライブ判定に使っていた要素が実は通常動画のプレーヤーにも存在していたため、すべての動画でライブ用のシーク操作を実行してしまっていました。
- 「常に最新位置から再生」をOFFにしても、ページを開いた直後の1回だけ設定が無視されてシークが実行されてしまう不具合を修正。

## [2.6.0] - 2026-08-10

### Added
- ポップアップのヘッダーに拡張機能名「YouTube Live Helper」を控えめに表示。

### Changed
- 設定トグル操作時のトースト通知を画面上部（ヘッダー付近）に表示するよう変更（従来は下部でトグルのラベルに重なり見づらかった）。

## [2.5.0] - 2026-08-10

### Added
- アンケート（チャット内のpoll）を自分の画面から自動で非表示にする機能。ポップアップの「アンケートを自動で非表示」トグル（既定ON）で切り替え可能。「すべて表示」モードでYouTubeが残す崩れたアンケート表示も解消します。

## [2.4.0] - 2026-08-10

### Added
- 配信主の固定メッセージ（ピン留めバナー）を自分の画面から自動で非表示にする機能。ポップアップの「固定メッセージを自動で非表示」トグル（既定ON）で切り替え可能。他の視聴者には影響しません。

## [2.3.0] - 2026-08-10

### Added
- チャット欄を「トップチャット」から「チャット（すべて表示）」へ自動で切り替える機能。ポップアップの「チャットを常に全表示」トグル（既定ON）で切り替え可能。

### Changed
- 拡張機能名を「YouTubeLiveHopper」から「YouTube Live Helper」に変更。

## [2.2.0] - 2026-08-09

### Removed
- 配信の経過時間表示機能（ちらつき・視認性・一部動画で表示されない問題により削除）。

## [2.1.0] - 2026-08-09

### Added
- 配信の経過時間表示機能。

## [2.0.0] - 2026-08-09

ライブ最新位置への自動シークのみを行う単機能ツールへ再設計。

### Removed
- キーワード検索、トピックチャンネル登録、チャンネルリスト、配信終了時の次チャンネルへの自動ホップ機能。

### Changed
- アイコンを単色グラデーションから、ライブ最新位置へのシークを表す線画アイコンに変更。

## [1.1.0] - 2026-06-08

### Changed
- UI全体の見直しと、不安定な挙動の改善。

## [1.0.0] - 2026-06-07

### Added
- 初回リリース。キーワード検索、トピックチャンネル登録、チャンネルリスト、配信終了時の自動ホップ、ライブ最新位置への自動シーク。
